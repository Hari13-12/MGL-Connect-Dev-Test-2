[alembic]
script_location = migrations
# This is intentionally an isolated database used only by migration validation.
# Migration runners supply their own isolated URL through ALEMBIC_DATABASE_URL.
sqlalchemy.url = postgresql+psycopg://registration_test:registration_test@127.0.0.1:5432/registration_test
